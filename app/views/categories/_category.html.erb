<div id="<%= dom_id category %>">
<div class="card container-fluid" style="max-width:319px;">
  <div class="card-body container-fluid">
<table class="table table-success table-striped table-sm">
  <thead>
    <tr>

    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= category.name %></td>
      <td><%= link_to t('.edit'), edit_category_path(category) %></td>
      <td>  <%= link_to t('.delete'), category_path(category), data: {
    turbo_method: :delete,
    turbo_confirm: t('common.confirm')
  } %></td>
  </tbody>
</table>
  </div>
  </div>


</div>
